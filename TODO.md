# TODO

## Plan-approved steps
- [x] Locate sign-up/sign-in gating logic (this repo’s auth gate is `LoginPage` + `UserContext`).
- [x] Add session-aware logic so when `currentUser` exists, the customer is shown “Already signed in”.
- [ ] Ensure/adjust redirect behavior if desired (currently shows message; no redirect).
- [ ] Run typecheck/lint/build (as available) to confirm no regressions.


